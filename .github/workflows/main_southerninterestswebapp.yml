# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

<<<<<<<< HEAD:.github/workflows/enhancement_remotedb_siwebapp2.yml
name: Build and deploy Python app to Azure Web App - siwebapp2
========
name: Build and deploy Python app to Azure Web App - southerninterestswebapp
>>>>>>>> origin/main:.github/workflows/main_southerninterestswebapp.yml

on:
  push:
    branches:
      - Enhancement_RemoteDb
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate
      
      - name: Install dependencies
        run: pip install -r requirements.txt
        
      # Optional: Add step to run tests here (PyTest, Django test suites, etc.)

      - name: Zip artifact for deployment
        run: zip release.zip ./* -r

      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            release.zip
            !venv/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Unzip artifact for deployment
        run: unzip release.zip

      
      - name: Login to Azure
        uses: azure/login@v2
        with:
<<<<<<<< HEAD:.github/workflows/enhancement_remotedb_siwebapp2.yml
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_1E183628CFB44407B5E32E144FF2F191 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_C6123C669B154F88AE8D41AF01B60DCE }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_4960211713C64AFEA1D1DD4B708C3706 }}
========
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_5EFB0E60BB82443F9A988964FE2D8A0B }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_3C996CEAD6514236A8759751735FE393 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_5995C281B89245B7818F4E1E25621BE3 }}
>>>>>>>> origin/main:.github/workflows/main_southerninterestswebapp.yml

      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
<<<<<<<< HEAD:.github/workflows/enhancement_remotedb_siwebapp2.yml
          app-name: 'siwebapp2'
========
          app-name: 'southerninterestswebapp'
>>>>>>>> origin/main:.github/workflows/main_southerninterestswebapp.yml
          slot-name: 'Production'
          